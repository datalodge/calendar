config:
  target: 'http://localhost:3002'
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - name: "Getting booking"
    flow:
    - get:
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get:
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get:
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get:
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}"
    - get: 
        url: "/api/bookingInfo/{{$randomNumber(1, 1000)}}" 